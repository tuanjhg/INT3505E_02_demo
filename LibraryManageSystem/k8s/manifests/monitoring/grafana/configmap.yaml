apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-config
  namespace: library-system
data:
  grafana.ini: |
    [server]
    root_url = http://grafana.library.local

    [security]
    admin_user = admin
    admin_password = ${GRAFANA_ADMIN_PASSWORD}

    [users]
    allow_sign_up = false